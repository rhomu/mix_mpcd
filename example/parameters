#
# this is an example runcard
#

# system size
L = [50, 50]

# number of steps
nsteps = 5000

# info steps
ninfo = 100

# time step
tau = 5e-2

# number of particle types
#ntypes = 3
ntypes = 2

# number of particles
#dens = [10, 10, 10]
dens = [10, 10]

# interaction matrix
#M = [[ .4, -.2, -.1 ], [ -.4, .2, -.1], [-.2, -.2, -.1]]
M = [[ .2, -.2],[-.2, .2]]
